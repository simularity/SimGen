huawei ~?
 type_a,
 type_b.

type_a !
 p = 100,
 t = 100;
 p := levy_flight(p, 0, 200),
 t := wander(t, 0, 200, 3),
 speed = 500 - p - t.

type_b !
 p = 100,
 t = 100;
 p := levy_flight(p, 0, 200),
 t := wander(t, 0, 200, 3),
 speed = 500 + clock() - p - t.

% this file has syntax errors
% type_a and type_b have no conditions
